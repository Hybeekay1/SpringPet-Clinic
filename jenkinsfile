@Library('my-shared-library') _
pipeline {
   agent any
   stages {
    stage('Git Checkout'){
      steps {
         git branch: 'main', url: 'https://github.com/Hybeekay1/SpringPet-Clinic.git'
            }
        }
    }
}
