@Library('my-shared-library') _
pipeline {
   agent any
   stages {
    stage('Git Checkout'){
      steps {
         script {
            gitCheckout{
              git branch: 'main'
              url: 'https://github.com/Hybeekay1/SpringPet-Clinic.git'
            }
         }
            }
        }
    }
}
